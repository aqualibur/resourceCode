server:
  port: 8002

spring:
  application:
    name: member-redis-provider

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
  instance:
    instance-id: redis8002
    prefer-ip-address: true